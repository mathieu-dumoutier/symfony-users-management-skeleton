{% extends 'base.html.twig' %}

{% block title %}{{ 'Email de réinitialisation du mot de passé envoyé'|trans }}{% endblock %}

{% block body %}
    <div class="bg-white px-6 py-24 sm:py-32 lg:px-8">
        <div class="mx-auto max-w-2xl">
            <div class="rounded-md bg-green-50 p-4">
                <div class="flex">
                    <div class="shrink-0">
                        <svg class="size-5 text-green-400" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true" data-slot="icon">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 1 0 0-16 8 8 0 0 0 0 16Zm3.857-9.809a.75.75 0 0 0-1.214-.882l-3.483 4.79-1.88-1.88a.75.75 0 1 0-1.06 1.061l2.5 2.5a.75.75 0 0 0 1.137-.089l4-5.5Z" clip-rule="evenodd" />
                        </svg>
                    </div>
                    <div class="ml-3">
                        <h3 class="text-sm font-medium text-green-800">
                            {{ 'Si un compte avec cet e-mail existe, alors un email vient d\être envoyé contenant un lien pour choisir un nouveau mot de passe.'|trans }}
                        </h3>
                        <div class="mt-2 text-sm text-green-700">
                            <p>{{ 'Ce lien expirera dans'|trans }} {{ reset_token.expirationMessageKey|trans(reset_token.expirationMessageData, 'ResetPasswordBundle') }}</p>
                            <p>{{ 'Si vous ne recevez pas un email, vérifiez votre dossier spam ou'|trans }} <a href="{{ path('app_forgot_password_request') }}">{{ 'réessayez'|trans }}</a>.</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
